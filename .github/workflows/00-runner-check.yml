name: Runner Check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: [self-hosted]

    steps:
      - name: Identidade do runner
        run: |
          whoami
          hostname
          uname -a

      - name: K3s workloads
        run: kubectl get pods -A

      - name: Kubernetes OK?
        run: |
          kubectl version --client
          kubectl get nodes
          kubectl get ns
