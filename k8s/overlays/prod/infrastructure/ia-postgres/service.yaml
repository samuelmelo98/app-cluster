apiVersion: v1
kind: Service
metadata:
  name: ia-postgres
  namespace: prod
spec:
  selector:
    app: ia-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
